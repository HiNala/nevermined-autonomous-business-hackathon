import { StudioPage } from "@/components/pages/studio-page";

export const metadata = {
  title: "Studio — Auto Business",
  description: "Interact with the Strategist and Researcher agents. Run the full pipeline or work with individual agents.",
};

export default function Page() {
  return <StudioPage />;
}
