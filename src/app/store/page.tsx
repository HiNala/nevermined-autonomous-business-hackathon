import { StorePage } from "@/components/pages/store-page";

export const metadata = {
  title: "Store — Auto Business",
  description: "Browse and order products from the autonomous Seller agent. Each order triggers a full multi-agent pipeline.",
};

export default function Page() {
  return <StorePage />;
}
