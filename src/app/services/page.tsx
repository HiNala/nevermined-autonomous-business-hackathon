import { ServicesPage } from "@/components/pages/services-page";

export const metadata = {
  title: "Services — Auto Business",
  description: "Research sprints, planning packs, and design specs — pay per deliverable.",
};

export default function Page() {
  return <ServicesPage />;
}
